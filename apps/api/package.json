{
  "name": "carbonbridge-api-prototype",
  "version": "2.0.0",
  "private": true,
  "description": "Self-contained CarbonBridge backend prototype for deterministic marketplace and assistant workflows",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  }
}
